<%= form_for(@unit_measurement) do |f| %>
  <% if @unit_measurement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unit_measurement.errors.count, "error") %> prohibited this unit_measurement from being saved:</h2>

      <ul>
      <% @unit_measurement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr>
      <td>Code</td>
      <td><%= f.number_field :code %></td>
    </tr>
    <tr>
      <td>Description</td>
      <td><%= f.text_field :desc %></td>
    </tr>
    <tr>
      <td><%= f.submit "Submit" %></td>
    </tr>
  </table>
<% end %>
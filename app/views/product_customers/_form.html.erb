<%= form_for(@product_customer) do |f| %>
  <% if @product_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_customer.errors.count, "error") %> prohibited this product_customer from being saved:</h2>

      <ul>
      <% @product_customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :trade_company_id %><br />
    <%= f.number_field :trade_company_id %>
  </div>
  <div class="field">
    <%= f.label :selling_price %><br />
    <%= f.text_field :selling_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

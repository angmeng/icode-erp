<table id="grid_h295" class="white-line-table target_table">
  <thead>
    <tr style="background-color: gainsboro;">
      <th>Product ID</th>
      <th>Old Unit Price</th>
      <th>Old Effective Date</th>
      <th>New Unit Price</th>
      <th>New Effective Date</th>
    </tr>
  </thead>

  <tbody>
    <% if price_control.price_control_items.present? %>

      <%# price_control.price_control_items.each_with_index do |item, index| %>
    
        <!--tr class="<%#= cycle('odd_line', 'even_line') %>">     
          
          <td align="center" width="400"><%#= select_tag       "datarow[#{index}][product_id]", options_for_select(product_finish_goods_with_combobox, item.product_id), :include_blank => true, :id => "kendo_combobox", :style => "width: 400px;", :onchange => "product_data(this)" %></td>
          <td align="center" width="150"></td>
          <td align="center" width="150"></td>
          <td align="center" width="150"><%#= text_field_tag   "datarow[#{index}][new_unit_price]", four_precision(item.new_unit_price), :id => "kendo_price" %></td>
          <td align="center" width="150"><%#= text_field_tag   "datarow[#{index}][new_eff_date]", item.new_eff_date.strftime("%d-%m-%Y"), :id => "kendo_date"  %></td>   
          
        </tr-->
    
      <%# end %>

    <% else %>
      
      <tr class="<%= cycle('odd_line', 'even_line') %>">
      
        <td align="center" width="400"><%= select_tag "datarow[0][product_id]", options_for_select(product_finish_goods_with_combobox), :include_blank => true, :style => "width: 400px;", :onchange => "price_control_data(this)" %></td>
        <td align="center" width="150">
          <span id="old_up_0"></span>
          <%= hidden_field_tag "datarow[0][old_unit_price]", '' %>
        </td>
        <td align="center" width="150">
          <span id="old_date_0"></span>
          <%= hidden_field_tag "datarow[0][old_eff_date]", '' %>
        </td>
        <td align="center" width="150"><%= text_field_tag   "datarow[0][new_unit_price]", 0.0000, :class => "four_precision" %></td>
        <td align="center" width="150"><%= text_field_tag   "datarow[0][new_eff_date]", Date.today.strftime("%d-%m-%Y") %></td>        
        
      </tr>

    <% end %>
  </tbody>
</table>

<%= form_for(@product_running_number) do |f| %>
  <% if @product_running_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_running_number.errors.count, "error") %> prohibited this product_running_number from being saved:</h2>

      <ul>
      <% @product_running_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :base_name %><br />
    <%= f.text_field :base_name %>
  </div>
  <div class="field">
    <%= f.label :sub_name %><br />
    <%= f.text_field :sub_name %>
  </div>
  <div class="field">
    <%= f.label :running_no %><br />
    <%= f.number_field :running_no %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

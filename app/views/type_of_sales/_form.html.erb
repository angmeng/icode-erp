<%= form_for(@type_of_sale) do |f| %>
  <% if @type_of_sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_of_sale.errors.count, "error") %> prohibited this type_of_sale from being saved:</h2>

      <ul>
      <% @type_of_sale.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

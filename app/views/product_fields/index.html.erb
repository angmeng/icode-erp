<h1>Listing product_fields</h1>

<table>
  <tr>
    <th>Category</th>
    <th>Field</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_fields.each do |product_field| %>
  <tr>
    <td><%= product_field.category_id %></td>
    <td><%= product_field.field_id %></td>
    <td><%= link_to 'Show', product_field %></td>
    <td><%= link_to 'Edit', edit_product_field_path(product_field) %></td>
    <td><%= link_to 'Destroy', product_field, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product field', new_product_field_path %>

<div class="left_icon_tag">
  <% if roles.include?(InventoryManagementSystem::TRANSFER_NOTES_NEW) %> 
    <li><%= link_to "Add - New Transfer Note", new_stock_out_path, :class => "show_without_refresh_page_800x450 k-button font_bold", "data-fancybox-type" => "iframe" %></li>
  <% else %>
    <li><%= disabled_menu_style "Add - New Transfer Note" %></li>
  <% end %>
</div>
  
<div class="icon_tag">
  
  <% if roles.include?(InventoryManagementSystem::TRANSFER_NOTES_SHOW) %>
    <span id="on_show" showClass="show_without_refresh_page_800x450"><%= not_link_show %></span>
  <% else %>
    <%= not_link_show %>
  <% end %>

  <!-- Histories can't EDIT module -->
  <%# if roles.include?(InventoryManagementSystem::TRANSFER_NOTES_EDIT) %>
    <!--span id="on_edit" editClass="iframe_1600x900"><%#= not_link_edit %></span-->
  <%# else %>
    <%= not_link_edit %>
  <%# end %>

  <% if roles.include?(InventoryManagementSystem::TRANSFER_NOTES_KIV) %>
     <span id="on_kiv"><%= not_link_kiv %></span>
  <% else %>
    <%= not_link_kiv %>
  <% end %>

  <% if roles.include?(InventoryManagementSystem::TRANSFER_NOTES_PRINTABLE) %>
    <span id="on_printable"><%= not_link_printable %></span>
  <% else %>
    <%= not_link_printable %>
  <% end %>
  
  <%= link_home_button %>

</div>
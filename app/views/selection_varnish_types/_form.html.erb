<%= form_for(@selection_varnish_type) do |f| %>
  <% if @selection_varnish_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@selection_varnish_type.errors.count, "error") %> prohibited this selection_varnish_type from being saved:</h2>

      <ul>
      <% @selection_varnish_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quotation_request_form_id %><br />
    <%= f.number_field :quotation_request_form_id %>
  </div>
  <div class="field">
    <%= f.label :varnish_type %><br />
    <%= f.text_field :varnish_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

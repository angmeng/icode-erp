<%= form_for(@inventory_management_system) do |f| %>
  <% if @inventory_management_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_management_system.errors.count, "error") %> prohibited this inventory_management_system from being saved:</h2>

      <ul>
      <% @inventory_management_system.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Name *" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

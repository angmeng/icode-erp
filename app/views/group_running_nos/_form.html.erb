<%= form_for(@group_running_no) do |f| %>
  <% if @group_running_no.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_running_no.errors.count, "error") %> prohibited this group_running_no from being saved:</h2>

      <ul>
      <% @group_running_no.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code, "Code *"%><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :current_no %><br />
    <%= f.number_field :current_no %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

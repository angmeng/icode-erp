<h2> Receive Note Report</h2>


<div>


		<% if @excel_receive_note_report.present? %>
	<table id="jgrid" class="zebraLight normal_table_black" >
		<thead>
			<tr>
				
        <th>Product Id / Description</th>
        <th>Received Qty</th>
        <th>P/O No.</th>
        <th>Part No.</th>				
			</tr>
		</thead>

		<tbody>
		<% @excel_receive_note_report.each do |ste| %>
			<tr>
			
      <td align="center"><%= render '/product_categories/join_product_category' , :pc => pri rescue'-' %> / <%= purchse_order_item_line_purchase_requisition_item_product.desc rescue '-' %></td>
        
      <%# d.received_note_items.each do |pri| %>
      <td align="center"><%= pri.received_qty rescue '-'%></td>
      <%# end %>
      <td align="center"><%= pri.purchase_order_item_line.purcahse_order.po_no rescue '-' %></td>
      <td align="center"><%= pri.purchase_order_item_line.purchase_requisition_item.part_no rescue '-' %></td>

			</tr>
			
		<% end %>
		

		
		</tbody>

		<% else %>
			no report found
		<% end %>
	</table>


	
  	

</div>

<style>

  table{
    width:100%;
  }
  
  body {
    width:100%;
    background-color: white;
    color: #333;
    font-family: "Courier New", Courier, monospace;
    font-size: 11px;
    line-height: 18px; 
    
  }

  p, div { margin: 0; }

  p, ol, ul, td {
    font-family: "Courier New", Courier, monospace;
    font-size: 11px; }

    table.black-line-table {
      border-width: 1px;
      <%#*border-spacing: 0px;%>
      border-style: none;
      border-color: black;
      border-collapse: collapse;
      background-color: transparent; }

  table.black-line-table th {
          border-width: 1px;
          padding: 1px;
          border-style: solid;
          border-color: black;
          background-color: transparent; }

  table.black-line-table td {
         
          border-width: 1px;
          padding: 1px;
          border-style: solid;
          border-color: black;

        }
    thead td {
        border-style: none none solid none;
        border-width: thick;
        border-color: red;
        margin: 4pt;
        border-bottom: 5pt short(4pt) solid red
    }

</style>

  <%= render "linking" %> 

  <div class="title_head"><strong style="color:white;">PRODUCT ID REGISTRATION (NON OPERATION AND OPERATION)</strong></div>


    <% if @no_product_id.present? %>
      <%= render "/purchase_orders/read_no_product_id", :product_id => @no_product_id %>
    <% else %>
      <div class="linking_content">No Record Found</div>
    <% end %>

  <%= render "action_purchase_order_no_product_id" %>
<%= render "linking" %> 

<div class="title_head"><strong style="color:white;">VENDOR REGISTRATION</strong></div>

<% if @vendor.present? %>
  <div class="table_color"><%= render "/purchase_orders/vendor", :pending => @vendor %></div>
<% else %>
  <div class="linking_content">No Record Found</div>
<% end %>

<%= render "action_purchase_order_vendor_reg" %>
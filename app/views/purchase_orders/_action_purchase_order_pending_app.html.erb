<div class="icon_tag">
  
  <% if roles.include?(InventoryManagementSystem::PURCHASE_ORDER_VENDOR_SELECTION_APPLY) %>
    <span id="on_apply_vendor_select" applyClass="iframe_800x600" ><%= not_link_apply %></span>
  <% else %>
    <span class="word_opac"><%= not_link_apply %></span>
  <% end %>
    
  <%# if roles.include?(InventoryManagementSystem::PURCHASE_ORDER_VENDOR_SELECTION_SUBMIT_TO) %>
    <%#*<span id="on_submit_to">%>
      <%#= not_link_submit_to %>
    <%#*</span>%>
  <%# else %>
    <span class="word_opac"><%= not_link_submit_to %></span>
  <%# end %>
   
  <%= link_home_button %>

</div>
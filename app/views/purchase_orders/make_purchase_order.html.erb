
  <%= render "linking" %> 

  <div class="title_head"><strong style="color:white;">PO ENTRY</strong></div>

    <% if @pr_items.present? %>
      <%= render "/purchase_orders/existing_approved_from_purchase_requisition_items", :pending => @pr_items %>
    <% else %>
      <div class="linking_content">No Record Found</div>
    <% end %>

  <%= render "action_purchase_order_make_po" %>
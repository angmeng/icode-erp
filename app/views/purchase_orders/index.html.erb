<%= render "linking" %>

<div class="title_head">
  <strong style="color:white;">TRANSACTIONS & HISTORIES FOR PURCHASE ORDERS</strong>
  <%= search_button %>
</div>
<%= render "search_po" %>

<% if @purchase_orders.present? %>
  <div class="table_background"><%= render "/purchase_orders/success_approved_from_purchase_requisition_items", :success => @purchase_orders %></div>
<% else %>
  <div class="linking_content">No Record Found</div>
<% end %>

<%= render "action_purchase_order_transaction" %>
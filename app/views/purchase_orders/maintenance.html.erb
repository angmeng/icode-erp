<%= render "linking" %>

<%= title_head "PR-REPAIR" %>

<% if @maintenance.present? %>
  <div class="table_background"><%= render "pr_repair", :maintenance => @maintenance %></div>
<% else %>
  <div class="linking_content">No Record Found</div>
<% end %>

<%= render "action_purchase_order_repair" %>
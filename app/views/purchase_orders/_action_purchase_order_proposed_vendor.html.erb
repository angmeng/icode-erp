<div class="icon_tag">
  
  <% if roles.include?(InventoryManagementSystem::PURCHASE_ORDER_PROPOSED_VENDOR_APPLY) %>
    <span id="on_apply_proposed_vendor" applyClass="iframe_800x600"><%= not_link_apply %></span>
  <% else %>
    <span class="word_opac"><%= not_link_apply %></span>
  <% end %>
    
  <% if roles.include?(InventoryManagementSystem::PURCHASE_ORDER_PROPOSED_VENDOR_SUBMIT_TO) %>
    <span id="on_submit_proposed_vendor"><%= not_link_submit_to %></span>
  <% else %>
    <span class="word_opac"><%= not_link_submit_to %></span>
  <% end %>
   
  <%= link_close_button %>

</div>
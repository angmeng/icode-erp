<div class="icon_tag">
  
  <% if roles.include?(InventoryManagementSystem::PURCHASE_ORDER_VENDOR_REGISTRATION_APPLY) %>
    <span id="on_apply_vendor_reg" applyClass="iframe_1600x900"><%= disabled_style "Add New Vendor" %></span>
  <% else %>
    <%= disabled_style "Add New Vendor" %>
  <% end %>
    
  <span><%= not_link_submit_to %></span>
   
  <%= link_home_button %>

</div>
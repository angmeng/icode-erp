<div class="icon_tag">
  
  <% if roles.include?(InventoryManagementSystem::PURCHASE_ORDER_VENDOR_REGISTRATION_APPLY) %>
    <span id="on_apply_vendor_reg" applyClass="iframe_1024x800"><%= not_link_apply %></span>
  <% else %>
    <span class="word_opac"><%= not_link_apply %></span>
  <% end %>
    
  <span class="word_opac"><%= not_link_submit_to %></span>
   
  <%= link_close_button %>

</div>
<%= render "linking" %>

<%= title_head "PROPOSED VENDOR" %>

<% if @proposed_vendor.present? %>
  <div class="table_background"><%= render "/purchase_orders/proposed_vendor_from_purchase_requisition_items", :proposed => @proposed_vendor %></div>
<% else %>
  <div class="linking_content">No Record Found</div>
<% end %>

<%= render "action_purchase_order_proposed_vendor" %>

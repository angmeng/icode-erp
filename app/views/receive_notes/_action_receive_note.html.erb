<div class="left_icon_tag">
  <% if roles.include?(InventoryManagementSystem::RECEIVE_NOTE_NEW) %>
    <%# if roles.include?(InventoryManagementSystem::RECEIVE_NOTE_NEW_ADD) %>
        <span><%= link_to "New RNs", new_receive_note_path, :class => "show_without_refresh_page_1600x900 k-button", "data-fancybox-type" => "iframe" %></span>
    <% else %>
        <span class = "k-button" style = "color: grey; background-color: white; cursor: default;">New RNs</span>
    <% end %>
  <%# end %>
</div>

<div class="icon_tag">
  
  <% if roles.include?(InventoryManagementSystem::RECEIVE_NOTE_TRANSACTION_SHOW) %>
    <span id="on_show" showClass="show_without_refresh_page_1600x900"><%= not_link_show %></span>
  <% else %>
    <span class="word_opac"><%= not_link_show %></span>
  <% end %>

  <% if roles.include?(InventoryManagementSystem::RECEIVE_NOTE_TRANSACTION_EDIT) %>
    <span id="on_edit" editClass="iframe_800x450"><%= not_link_edit %></span>
  <% else %>
    <span class="word_opac"><%= not_link_edit %></span>
  <% end %>

  <% if roles.include?(InventoryManagementSystem::RECEIVE_NOTE_TRANSACTION_KIV) %>
     <span id="on_kiv"><%= not_link_kiv %></span>
  <% else %>
    <span class="word_opac"><%= not_link_kiv %></span>
  <% end %>

  <% if roles.include?(InventoryManagementSystem::RECEIVE_NOTE_TRANSACTION_PRINTABLE) %>
    <span id="on_printable"><%= not_link_printable %></span>
  <% else %>
    <span class="word_opac"><%= not_link_printable %></span>
  <% end %>
   
  <%= link_home_button %>

</div>